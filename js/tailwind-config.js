// Tailwind CDN configuration moved from inline script
// must run after the CDN script is loaded

tailwind.config = {
    theme: {
        extend: {
            colors: {
                'primary': '#10b981',
                'primary-dark': '#059669',
                'secondary': '#14b8a6',
                'accent': '#06b6d4',
            }
        }
    }
};